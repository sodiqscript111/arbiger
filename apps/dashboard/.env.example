# Base URL of the arbiger ingestion API (no trailing slash).
# The dashboard reads this at build/dev time. You can also override it at
# runtime from the Connect screen (persisted to localStorage).
VITE_API_BASE=http://localhost:3000
